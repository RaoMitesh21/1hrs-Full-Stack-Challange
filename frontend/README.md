AI Interview Simulator - Frontend

Setup

1. Install dependencies

   npm install

2. Run dev server

   npm run dev

Notes

- The frontend expects the backend to be running at http://localhost:5000
- Auth token is stored in localStorage as `ai_token`
- Built with Vite + React, no UI libraries. Charts use Chart.js via react-chartjs-2.
